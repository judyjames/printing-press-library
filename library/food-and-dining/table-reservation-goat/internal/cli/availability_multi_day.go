// Copyright 2026 pejman-pour-moezzi. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAvailabilityMultiDayCmd(flags *rootFlags) *cobra.Command {
	var flagStartDate string
	var flagDays int
	var flagPartySize int

	cmd := &cobra.Command{
		Use:   "multi-day <restaurant>",
		Short: "Multi-day availability for a single restaurant — Mon-Sun matrix",
		Example: "  table-reservation-goat-pp-cli availability multi-day example-value --start-date 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "availability.multi_day", "pp:method": "GET", "pp:path": "/availability/multi-day", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("start-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-date")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/availability/multi-day"
			params := map[string]string{}
			params["restaurant"] = args[0]
			if flagStartDate != "" {
				params["start_date"] = fmt.Sprintf("%v", flagStartDate)
			}
			if flagDays != 0 {
				params["days"] = fmt.Sprintf("%v", flagDays)
			}
			if flagPartySize != 0 {
				params["party_size"] = fmt.Sprintf("%v", flagPartySize)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "availability", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagStartDate, "start-date", "", "Start of date range (YYYY-MM-DD)")
	cmd.Flags().IntVar(&flagDays, "days", 0, "Number of days to scan (default 7, max 14)")
	cmd.Flags().IntVar(&flagPartySize, "party", 0, "Party size (default 2)")

	return cmd
}
