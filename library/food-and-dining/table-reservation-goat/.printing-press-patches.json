{
  "schema_version": 1,
  "applied_at": "2026-05-09",
  "base_run_id": "20260509-110540",
  "base_printing_press_version": "4.2.1",
  "patches": [
    {
      "id": "cross-network-source-clients",
      "summary": "Hand-written Surf-based source clients for OpenTable (GraphQL + SSR) and Tock (REST + SSR), plus a shared session-cookie store imported from local Chrome via kooky.",
      "reason": "Both consumer sites sit behind Akamai/Cloudflare bot defense; Surf's Chrome TLS fingerprint clears them. The OpenTable Partner API is gated and Tock has no public REST API, so the only viable path is the consumer-side surface \u2014 there's no upstream spec to generate from, so these are net-new packages outside the generator's control. The packages live in `internal/source/{auth,opentable,tock}/` and back the goat/watch/earliest/drift commands.",
      "files": [
        "internal/source/auth/auth.go",
        "internal/source/auth/chrome.go",
        "internal/source/opentable/client.go",
        "internal/source/opentable/ssr.go",
        "internal/source/tock/client.go",
        "internal/source/jseval/jseval.go"
      ],
      "validated_outcome": "Live test: 24 OpenTable cookies + 29 Tock cookies imported via `auth login --chrome`; `auth status` reports both networks logged_in: true. v0.1.1: goja-based JS evaluator (jseval) replaced naive balanced-brace + regex strip; live `goat 'canlis'` now returns Canlis on both networks (Tock id=4891, OT id=6309). v0.1.2: AdaptiveLimiter + typed RateLimitError wired into both source clients (do429Aware helper) \u2014 single 429 retry with Retry-After backoff, then surface as hard error so empty-on-throttle stops being indistinguishable from 'no data exists'. OT floor 0.5 req/s, Tock 1.0 req/s; both ramp via OnSuccess after 10 consecutive successes. v0.1.3 (Greptile review): SearchRestaurants default DateTime now computed at call time (today+7d at 19:00); Bootstrap dedupes concurrent fetches via singleflight."
    },
    {
      "id": "novel-commands",
      "summary": "Five hand-written novel commands (auth, goat, watch, earliest, drift) registered in root.go, implementing the cross-network unified search, cancellation watcher, multi-venue earliest-available, and per-venue change feed.",
      "reason": "Phase 1.5 absorb manifest approved a tight-v1 with 4 transcendence features (goat, watch, earliest, drift) plus auth login --chrome cookie import. None of these have a generator-template equivalent \u2014 they require local SQLite (watch), cross-network composition (goat, earliest), and snapshot diffing (drift). root.go is modified to register them.",
      "files": [
        "internal/cli/auth.go",
        "internal/cli/goat.go",
        "internal/cli/watch.go",
        "internal/cli/earliest.go",
        "internal/cli/drift.go",
        "internal/cli/root.go"
      ],
      "validated_outcome": "Phase 5 live dogfood: 64/64 PASS. All four transcendence commands return well-shaped JSON in dry-run; live-data paths surface honest errors when SSR JS-state can't be fully parsed (documented v0.2 gap). v0.1.3 (Greptile review): removed misleading mcp:read-only on `auth` parent group and `auth login` (login writes cookies to disk); `watch tick` no longer reports Polled=true on the OT branch (honest no-op until v0.2 RestaurantsAvailability bootstrap); `watch add` refuses opentable-prefixed slugs with a clear v0.2 error rather than storing a watch that silently never matches. v0.1.4 (Greptile follow-up): removed misleading mcp:read-only on `drift` (writes snapshot files); reordered earliest's network preference to try Tock first (real availability) before OT, with the OT branch surfacing an honest 'venue exists, availability watching is v0.2 work' rather than a misleading Available=false."
    },
    {
      "id": "scaffold-endpoint-redirects",
      "summary": "Generated scaffold endpoints (restaurants list/get, availability check) rewritten to delegate to the cross-network source clients so they hit real Surf-cleared SSR data instead of opentable.com placeholder paths that 404.",
      "reason": "The internal YAML spec defines /restaurants and /availability as REST paths with placeholder paths because OpenTable doesn't expose REST equivalents. The generated scaffold's client.Get(...) calls 404 against the live consumer site. Without these redirects the resource-style commands would always fail \u2014 and the user's headline workflow (find a restaurant, check availability) needs them to work. Each rewrite preserves the spec annotations (pp:endpoint, pp:method, pp:path, mcp:read-only) so the typed MCP surface keeps working; only RunE is changed.",
      "files": [
        "internal/cli/restaurants_list.go",
        "internal/cli/restaurants_get.go",
        "internal/cli/availability_check.go",
        "internal/cli/sync.go"
      ],
      "validated_outcome": "Live dogfood went from 16/78 failures (placeholder 404s) to 0/64 failures after the redirects landed. `sync` returns an empty success summary instead of erroring with stream issues; `restaurants list/get` and `availability check` route through the same Surf-cleared path goat/earliest use.",
      "upstream_issue": "Multiple gaps surfaced as Printing Press retro candidates: (1) generator emits unused helpers in helpers.go; (2) dogfood reimplementation_check sibling-import regex misses multi-segment internal/source/* paths; (3) generator does not emit .printing-press-patches.json on fresh print despite library CI requiring it."
    }
  ]
}